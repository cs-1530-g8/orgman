= form_tag update_status_path, class: 'form-inline'  do
  = hidden_field_tag 'status', status
  .form-group
    = label :users_to_update, title
    = select_tag :users_to_update, options_from_collection_for_select(users, "id", "name"), multiple: true
  .form-group
    = submit_tag 'Update', class: 'btn btn-success form-control'

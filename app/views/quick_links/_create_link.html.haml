=form_for(@new_link, url: create_quick_link_path) do |f|

  = f.label :name, 'Link Name'
  = f.text_field :name

  = f.label :url, 'Link URL'
  = f.text_field :url

  = f.label :expiration, 'Expiration Date'
  = f.date_field :expiration

  = f.submit

